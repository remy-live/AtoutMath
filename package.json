{
  "name": "atoutmath",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Entraînement et évaluation en mathématiques (collège) — application web sans build.",
  "scripts": {
    "test": "node --test tests/",
    "serve": "python3 -m http.server 8080",
    "balayage": "node outils/balayage.mjs"
  }
}
